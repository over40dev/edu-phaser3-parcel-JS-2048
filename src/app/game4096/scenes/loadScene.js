// bootScene.js
import Phaser from 'phaser';
import {CST} from '../../../constants';

export class loadGame extends Phaser.Scene {
  constructor() {
    super({
      key: CST.scenes.LOAD
    });
  }

  create() {
  }
}

